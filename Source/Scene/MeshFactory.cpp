#include "Scene/MeshFactory.h"
#include "Common/Common.h"

Mesh* MeshFactory::CreateSphere(VertexDataFlags dataFlags)
{
	assert(false);
	return nullptr;
}

Mesh* MeshFactory::CreateTorus(VertexDataFlags dataFlags)
{
	assert(false);
	return nullptr;
}

Mesh* MeshFactory::CreateCube(VertexDataFlags dataFlags)
{
	assert(false);
	return nullptr;
}

Mesh* MeshFactory::CreateCylinder(VertexDataFlags dataFlags)
{
	assert(false);
	return nullptr;
}
